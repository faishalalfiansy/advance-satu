const Lupapass = (props) => {
    const { children } = props
    return (
        <section id="lupa">
              <a href="#" className='forget'>{children}</a>
            </section>
    )
}

export default Lupapass